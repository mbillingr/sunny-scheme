(declare-library-exports (native base)
  (export
    = > < - +
    apply
    car caar cadr
    cdr cdar cddr
    char? close-port cons
    eof-object?
    eq? equal?
    error
    list->string
    null?
    pair?
    set-car! set-cdr!
    string->list string-cons string<?
    symbol? symbol->string))
